# Phase 6: 機能と実装（詳細設計）の関係図

このドキュメントは、**Phase 6: Langfuse LLMOps 品質監視** における「要件」「機能」、そしてその裏で職人（AI）がどのような意図・設計ポイントで「実装（詳細設計）」を行っているかを現場監督が俯瞰するための関係図です。
（※黒色背景のエディタで視認しやすいダークテーマを適用しています。）

```mermaid
%%{init: {'theme': 'dark'}}%%
graph LR
    %% クラス定義（ダークモード向けの配色）
    classDef req fill:#1e3a8a,stroke:#60a5fa,stroke-width:2px,color:#fff
    classDef func fill:#064e3b,stroke:#34d399,stroke-width:2px,color:#ecfdf5
    classDef impl fill:#3f2a15,stroke:#f59e0b,stroke-width:1.5px,color:#fef3c7

    %% 左: 要件
    R1("要件1:<br>実行とコストの一元可視化")
    R2("要件2:<br>ユーザー評価の収集と紐付")
    R3("要件3:<br>会話の文脈管理")
    R4("要件4:<br>運用データからテスト・改善")

    %% 中央: 機能
    R1 --> F1("機能: 環境準備・SSM連携")
    R1 --> F2("機能: トレースの可視化連携")
    R1 --> F3("機能: トークン・コスト自動計算")

    R2 --> F4("機能: Good/Bad スコアリングAPI")
    
    R3 --> F5("機能: 長期セッション管理")
    
    R4 --> F6("機能: データセットとテスト比較")

    %% 右: 実装ポイント（現場監督向けの説明）
    F1 --> I1("【実装ポイント: 認証情報の保護】<br>LangfuseのAPIキー等をAWSのSSMに保存。<br>起動時に環境変数に取り込み、<br>コードへの平文ベタ書きを防止する。")
    F2 --> I2("【実装ポイント: 監視用ガードレール】<br>Bedrock呼び出し前後に記録処理を挟む。<br>※Langfuse側が障害を起こしても、<br>本番アプリのチャット応答が止まらないよう<br>例外処理（try-except）を徹底する。")
    F3 --> I3("【実装ポイント: 利用量の確実な集計】<br>Bedrockから返却される 'usage' <br>（入力・出力トークン数）を明示的に取得し、<br>生成ログ（Generation）に添付して送信する。")
    
    F4 --> I4("【実装ポイント: エンドポイント新設】<br>API Gateway + Lambda に /feedback を追加。<br>送られてきた 'trace_id' に対して、<br>Good=1 / Bad=0 というスコア情報を<br>Langfuse API経由で直接レコードに紐付ける。")
    
    F5 --> I5("【実装ポイント: IDの引き継ぎ】<br>フロントエンド（Dify等）の会話IDを<br>Langfuseの 'session_id' として設定。<br>単発のAPI呼び出しを一連の文脈（スレッド）<br>としてダッシュボード上でグループ化する。")
    
    F6 --> I6("【実装ポイント: PDCA基盤の構築】<br>本番で集めた良質な実データ（トレース）から<br>テスト用データセットを作成抽出。<br>プロンプト改修時にまとめて走らせ（実験）、<br>改修前後の品質低下がないかを定量評価する。")

    %% クラスの割り当て
    class R1,R2,R3,R4 req
    class F1,F2,F3,F4,F5,F6 func
    class I1,I2,I3,I4,I5,I6 impl
```

### 現場監督（マネージャー）としてのチェックポイント
この図の右側のオレンジ色の枠「実装ポイント」は、**職人がコードを書くときに意識すべき要所**を表しています。
レビューやテストを行う際は、主に以下の点を確認してください。

1. **SSM・環境変数**: セキュリティ事故を防ぐため、キーがリポジトリに含まれていないか。
2. **例外処理（可用性確保）**: 監視ツールが落ちている時にサービス全体が巻き添えで落ちる設計になっていないか。
3. **正確なトレースID紐付け**: 評価API（`/feedback`）が、正しくチャット本体のIDを受け取りLangfuseに流せているか。
4. **実験による定量比較**: 感覚ではなく「データセットに基づく前後のスコア比較」ができているか。
