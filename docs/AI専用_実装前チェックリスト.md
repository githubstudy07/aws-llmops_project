# ⚠️ AI実装前 強制チェックリスト (MANDATORY PRE-IMPLEMENTATION PROTOCOL)

> **[CRITICAL ORDER]** このファイルを読んでいるAIへ。以下は「お願い」ではなく「命令」だ。実装の1行目を書く前に、このリストをすべて実行し、その結果をユーザーに報告せよ。省略・要約は「重大な契約違反」として扱う。

---

## 🔴 STEP 1: 今日の失敗事例から学べ（必読）

過去のAIは、Langfuse の `CallbackHandler` を有効化するために **1時間・15回以上のデプロイを無駄にした**。
その原因は、「古い知識で独力解決しようとした」ことだ。この過ちを繰り返すな。

**お前がこれからしようとしていることを、頭の中でシミュレーションせよ。**
- そのライブラリ・APIは、バージョンアップで仕様が変わっていないか？
- `requirements.txt` に必要な依存パッケージはすべて含まれているか？
- 公式ドキュメントを確認したか？（記憶だけで書くな）

---

## 🟠 STEP 2: 3つの「やるな」宣言

実装中、以下の行動が頭をよぎった瞬間に、**即座に手を止めてユーザーに報告せよ。**

| # | ⛔ やってはいけないこと | なぜ禁止か |
|---|---|---|
| 1 | `os.walk` などでLambda内部のディレクトリを物理的に検索する | ライブラリが見つけられないのは「コードの問題」ではなく「依存関係か仕様の問題」。力技は時間の浪費。 |
| 2 | `try-except` の中に空のMockクラスを作って「動いているように見せる」 | 問題を隠蔽しているだけで、本質の解決になっていない。ユーザーを欺くことになる。 |
| 3 | 3回連続で同じエラーが出ているのに、同じ方向でコードを修正し続ける | それは「間違った地図で歩き続けている」状態だ。地図（情報ソース）を変えろ。 |

---

## 🟡 STEP 3: 「2回失敗ルール」の宣誓

> **お前はこれを声に出せ（報告文として出力せよ）：**
> 「私は、同じエラーや問題解決に2回失敗した場合、3回目の試みをする前に必ずWeb検索機能を使用して最新の公式ドキュメントを確認します。記憶や推測で3回目を試みることは、このプロジェクトでは禁止されています。」

---

## 🟢 STEP 4: 実装前の最終確認リスト

以下を確認し、**「OK」か「要確認」かをユーザーに報告してから実装を開始せよ。**

- [ ] 使用するライブラリの**最新バージョン**と**正しいインポートパス**をWeb検索または公式ドキュメントで確認した
- [ ] `requirements.txt` に必要なパッケージ（依存先も含む）がすべて記載されている
- [ ] 実装しようとしている機能を、**一言で説明できる**（説明できないなら、まだ理解が足りない）
- [ ] エラーが起きた時の「切り分けポイント」をあらかじめ考えている
- [ ] このドキュメントの内容をすべて読み終えた

---

## 🧐 ケーススタディ：泥沼 vs クリーン (Evidence)

お前がこれから書くコードが「泥沼」か「クリーン」か、以下の実例（Session 12-13の修正）を見て判断せよ。

### ❌ 泥沼コード (ハマっている時のAIの末路)
```python
# ファイルが見つからないからといって物理探索を始める
for root, dirs, files in os.walk("/var/task/langfuse"):
    if "callback.py" in files:
        found_path = os.path.join(root, "callback.py")
        break
# 実態のない空のクラスでエラーを誤魔化す
class CallbackHandler:
    def __init__(self, *args, **kwargs): pass
```
**判定：退場。** ライブラリの仕様（インポートパス）が間違っているか、`requirements.txt` が不足していることに気づけていない。

### ✅ クリーンコード (正しい実装)
```python
# 正しいインポートパスをWeb検索で特定し、依存関係を requirements に追加
from langfuse.langchain import CallbackHandler

# 環境変数（os.environ）を活用し、初期化をシンプルに保つ
if CallbackHandler and LANGFUSE_CONF:
    handler = CallbackHandler(session_id=thread_id)
```
**判定：合格。** シンプルであり、環境固有の制約（Lambdaのパス等）に依存せず、ライブラリの標準に従っている。

---

## 📌 付記：現場監督（ユーザー）へ

このファイルを読み込ませるだけでOKです。AIに以下の1行を指示するだけで、このプロトコルが発動します。

> 「`docs/AI専用_実装前チェックリスト.md` を読み込んで、STEP 1〜4をすべて実行してから実装を開始して。」

**管理ファイルはこの1ファイルだけです。**
