# システム全体 機能間連携プロセス図

この図は、これまでの開発（Phase 4～6）で細分化して実装してきた「個別の機能（チェックリストの項目）」が、最終的に**1つの巨大なシステムとしてどのように連携・連動して動いているか**を示すプロセス図です。

ユーザーからの入力フロー、AWS内部の処理フロー、Langfuseの監視フロー、そしてGitHubによる開発・デプロイ運用フローというプロセス全体において、各機能設計が「どの役割を担っているか」をフェーズと要件番号を付けて可視化しています。

```mermaid
%%{init: {'theme': 'dark'}}%%
graph TD
    classDef cd_user fill:#2d3748,stroke:#a0aec0,stroke-width:2px,color:#fff
    classDef cd_gw fill:#9c4221,stroke:#fbd38d,stroke-width:2px,color:#fff
    classDef cd_comp fill:#2b6cb0,stroke:#63b3ed,stroke-width:2px,color:#fff
    classDef cd_db fill:#22543d,stroke:#68d391,stroke-width:2px,color:#fff
    classDef cd_llm fill:#553c9a,stroke:#b794f4,stroke-width:2px,color:#fff
    classDef cd_ci fill:#1a202c,stroke:#718096,stroke-width:2px,color:#cbd5e0

    %% =======================
    %% ユーザー＆フロントエンド
    %% =======================
    User(("👩‍💻 エンドユーザー"))
    Dify["[P4.2/要件1/機能1, 2]<br>外部UI (Dify等)<br>接続確認と会話ID送信"]
    
    User -->|"1. チャット入力"| Dify
    User -->|"8. Good/Bad評価"| Dify

    %% =======================
    %% インフラ基盤・エントリ
    %% =======================
    subgraph "AWS クラウド環境全体"
        APIGW_Chat["[P4.1/要件2/機能5]<br>API Gateway (/chat)<br>APIアクセス保護"]
        APIGW_Feed["[P6/要件2/機能4 関連]<br>API Gateway (/feedback)<br>評価動的エンドポイント"]
        
        Dify -->|"2. HTTP POST (Chat)"| APIGW_Chat
        Dify -->|"9. HTTP POST (Feedback)"| APIGW_Feed
        
        %% コンピューティング処理
        Lambda["[P4.1/要件1/機能1]<br>Lambda (バックエンド)<br>システム自律稼働"]
        APIGW_Chat --> Lambda
        APIGW_Feed --> Lambda
        
        Lambda -->|"6. 回答返却"| APIGW_Chat
        APIGW_Chat -->|"7. 回答表示"| Dify
        
        %% プロジェクト共通リソース
        IAM["[P4.1/要件2/機能3]<br>IAM<br>最小権限の保護"] 
        IAM -.->|"実行許可"| Lambda
        
        SSM["[P4.1/要件3/機能4] 環境変数<br>+[P6/要件1/機能1] SSM連携<br>暗号化キー読み込み"]
        SSM -.->|"設定取得"| Lambda
        
        Dynamo["[P4.1/要件1/機能2]<br>DynamoDB<br>文脈の保存と呼び出し"]
        Lambda <-->|"3. 履歴DBアクセス"| Dynamo
        
        Bedrock["[P4.2/要件2/機能3, 4]<br>Amazon Bedrock<br>指定モデル呼出・エラー保護"]
        Lambda <-->|"4. 生成リクエスト"| Bedrock
    end

    %% =======================
    %% Langfuse LLMOps 系
    %% =======================
    subgraph "Langfuse LLMOps (監視・改善)"
        LF_Trace["[P6/要件1/機能2]<br>トレーシング<br>プロンプト・回答内容記録"]
        LF_Cost["[P6/要件1/機能3]<br>コスト自動計算<br>Bedrock Token解析"]
        LF_Session["[P6/要件3/機能5]<br>セッション管理<br>会話をスレッド化"]
        LF_Score["[P6/要件2/機能4]<br>スコアリング処理<br>外部評価をTraceに紐付け"]
        LF_Eval["[P6/要件4/機能6]<br>データセット & 実験<br>実データで品質評価テスト"]
        
        Lambda -.->|"5. トレース送信"| LF_Trace
        LF_Trace --> LF_Session
        LF_Trace --> LF_Cost
        
        Lambda -.->|"10. スコア送信"| LF_Score
        LF_Score -.->|"Trace IDで結合"| LF_Trace
        
        LF_Trace -.->|"将来の改善のため抽出"| LF_Eval
    end

    %% =======================
    %% CI/CD GitHub 開発フロー
    %% =======================
    subgraph "GitHub Actions (CI/CD自動化)"
        Dependabot["[P5/要件3/機能5]<br>Dependabot<br>定期セキュリティスキャン"]
        GitRepo["[P5/要件1/機能0, 3, 4]<br>GitHub リポジトリ<br>OIDC連携と公開保護"]
        ActionsTest["[P5/要件3/機能7]<br>GitHub Actions<br>コミット時自動テスト(Pytest)"]
        ActionsDeploy["[P5/要件2/機能1, 2, 6]<br>自動/手動デプロイ<br>+[P5/要件4/機能8] 二重起動防止"]
        
        Dependabot -.->|"脆弱性検知"| GitRepo
        GitRepo -->|"Pushイベント等"| ActionsTest
        ActionsTest -->|"テスト通過"| ActionsDeploy
        ActionsDeploy -.->|"クラウドを自動構築・更新"| IAM
        ActionsDeploy -.->|"クラウドを自動構築・更新"| Lambda
        ActionsDeploy -.->|"クラウドを自動構築・更新"| APIGW_Chat
    end

    %% クラス適用（スタイル）
    class User cd_user
    class Dify,APIGW_Chat,APIGW_Feed cd_gw
    class Lambda cd_comp
    class Dynamo,SSM,IAM cd_db
    class Bedrock,LF_Trace,LF_Cost,LF_Session,LF_Score,LF_Eval cd_llm
    class GitRepo,ActionsTest,ActionsDeploy,Dependabot cd_ci
```

### 図の読み解き方
*   **実線矢印（-->）**：システムが実行される際の、「主要な通信フロー」や「処理の順序（例：1～10）」です。
*   **点線矢印（-.->）**：「裏側の非同期通信（ログ転送）」や「設定・権限の反映」など、システムが依存する関係性を表しています。
*   **ブロックの記述（例：`[P4.1/要件1/機能1]`）**：このコンポーネントが、ハンズオンのどのフェーズ・要件に紐付いて実装されたかを示します。

これを見ることで、例えば「Phase 5のCI/CD要件を実装したことで、AWSだけでなくリポジトリ全体の安全性も担保されている」ことや、「Phase 6で実装した複数のLangfuse機能が、Traceを中心に集約され、最終的に『改善テスト（データセット実験）』へと結びついている」といった、**「線と面で捉えたアーキテクチャの全体像」**を意識してマネジメントできるようになります。
